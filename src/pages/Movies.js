import MoviesList from "../components/MoviesList";

const Movies = () => {
  return (
    <div>
      MoviesList,search
      <div>sort by, filter</div>
      <MoviesList />
    </div>
  );
};

export default Movies;
